<% if current_user.teacher %>
  <% @courses.each do  |course| %>
    <p> <%= course.name %> - <%= link_to 'destroy', course_path(course), method: :delete, data: { confirm: "Are you sure you would like to delete?" } %></p>
  <% end %>
<% else %>
  <h2>you cannot view this page</h2>
<% end %>

